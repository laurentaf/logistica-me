name: 'logistica_dbt'
version: '1.0.0'

profile: 'logistica_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  logistica_dbt:
    +enabled: true

models:
  logistica_dbt:
    +materialized: view
